# Home Assistant connection (CLI / standalone mode)
GATEKEEPER_HA_URL=http://localhost:8123
GATEKEEPER_HA_TOKEN=
# LLM backend: claude (default) or local
GATEKEEPER_BACKEND=claude
# Cloud backend:
ANTHROPIC_API_KEY=
# Local backend (Ollama, OpenAI-compatible) uses GATEKEEPER_BACKEND=local
